/**Instructions to build and run the Auto-Management application
1. The main class is AutoMain
2. If you have not built the database object yet, enter 0 when prompted on running AutoMain

Operation of Application
1. On startup, the user is asked if the the db object needs to created
2. If needed, the application creates all tables and populates them from the included csv files
3. Then a command line interface is used to run the application and communicate with the user
*/